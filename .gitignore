node_modules/
dist/
dev-dist/
*.local

# Firebase client config — never commit real values
.env
.env.*
!.env.example

# local tooling settings
.claude/settings.local.json

# personal budget data — must never be committed to a public repo
seed-data.json
*backup*.json
